/// animated_loader
///
/// A Flutter package that provides a flexible, highly customizable loading indicator
/// widget. Supports both determinate (0.0–1.0) and indeterminate modes, six built-in
/// animation styles, and complete control over size, color palette, animation curves,
/// duration, auto-reverse behavior, and repeat counts.
///
/// ## Import
/// ```dart
/// import 'package:animated_loader/animated_loader.dart';
/// ```
library;

export 'src/animated_loader.dart';
